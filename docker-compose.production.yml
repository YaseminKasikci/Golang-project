services:
  server:
    build:
      context: ./
      dockerfile: Dockerfile
    restart: always
    volumes:
      - ./images:/app/images
    #this is for testing purpose.
    # TO DO: Remove before deploying
    ports:
      - 3000:3000
    depends_on:
      - db
